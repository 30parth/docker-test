services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-render-app
    ports:
      - "8080:80"
    environment:
      - PORT=80
      - DB_CONNECTION=sqlite
    env_file:
      - .env
